# Core & GUI
pyside6
markdown
keyring
pyyaml
pydantic

# LLM / AI
openai
langchain
langchain-openai
langchain-community
mcp
langchain-mcp-adapters

# Document Processing
pypdf
pdfplumber
python-docx
openpyxl
pypandoc
weasyprint

# Web Scraping & Search
beautifulsoup4
requests>=2.31.0
urllib3>=1.26.0,<2.3.0
chardet>=5.0.0,<6.0.0
charset-normalizer>=3.0.0,<4.0.0
readability-lxml
ddgs

# Data & Visualization
matplotlib
numpy
pandas

# Utilities
pygments
pynput
sounddevice
soundfile

# ---------------------------------------------------------------------------
# Smart Document Intelligence (OPTIONAL — heavy dependency, ~1 GB)
# Enables: smart_read_document, translate_document, extract_document_images
# These tools use AI-powered layout analysis to parse PDFs and DOCX files
# "like a human" — understanding structure, tables, and images.
#
# To activate, install manually:
#   pip install docling pillow
#
# The agent will auto-detect docling and enable the tools at startup.
# Without docling, the app works normally with standard read_pdf/read_docx.
# ---------------------------------------------------------------------------
